# Google Maps API Key
# Obtenha sua chave em: https://console.cloud.google.com/google/maps-apis
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# --- GCP / BigQuery (dados históricos de chuvas) ---
# Usado pela Netlify Function netlify/functions/historical-rain.js
# Nunca commitar valores reais; em produção use variáveis no painel da Netlify.

# Conteúdo do credentials.json em uma linha (JSON stringificado)
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"alertadb-cor",...}

# Ou, para desenvolvimento local, caminho para o arquivo:
# GOOGLE_APPLICATION_CREDENTIALS=./credentials/credentials.json
# Se não definir essa variável, a função tenta automaticamente:
# ./credentials/credentials.json

# Projeto GCP (opcional; pode vir do JSON)
GCP_PROJECT_ID=alertadb-cor

# Dataset e tabela no BigQuery onde estão os dados históricos de chuvas
BIGQUERY_DATASET=alertadb_cor_raw
BIGQUERY_TABLE=pluviometricos

# Colunas padrão do schema do COR
BIGQUERY_DATE_COLUMN=dia
BIGQUERY_STATION_ID_COLUMN=estacao_id
BIGQUERY_STATION_NAME_COLUMN=estacao
# Opcional: colunas para SELECT DISTINCT da função histórica
# BIGQUERY_SELECT_COLUMNS=`dia` AS dia, dia_original, utc_offset, m05, m10, m15, h01, h04, h24, h96, estacao, estacao_id

# Região do dataset (ex: southamerica-east1). Necessário se não estiver em US.
BIGQUERY_LOCATION=us-west1

# --- Desenvolvimento local (Vite) ---
# Para o modo Histórico (GCP) não retornar 404 ao rodar só "npm run dev", use uma das opções:
# 1) Rodar "npx netlify dev" (proxy usa http://127.0.0.1:8888), ou
# 2) Definir a URL do site no Netlify para o proxy encaminhar as chamadas ao GCP:
# VITE_HISTORICAL_RAIN_PROXY=https://chovendo-agora.netlify.app
